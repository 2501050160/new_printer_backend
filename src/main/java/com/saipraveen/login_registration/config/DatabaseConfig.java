package com.saipraveen.login_registration.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Spring Boot AutoConfiguration manages HikariDataSource cleanly via properties set in application.properties and main method.
}
